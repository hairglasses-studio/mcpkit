# Audit Findings — 2026-04-03

> Source: Claude Code Opus 4.6 1M context audit sweep
> Generated: 2026-04-03
> Repo: mcpkit
> Usage: `claude -p "Read .claude/audit-2026-04-03.md and fix all HIGH severity items"`

---

## Summary

mcpkit is a well-architected, thoroughly tested Go toolkit with 33+ packages at 90%+ coverage and clean dependency layering. The codebase is production-grade with strong patterns (middleware chains, typed handlers, thread-safe registries). The single highest-priority improvement is **fixing silent audit event loss in `security/audit.go`** — a security-critical package that silently drops events on queue overflow and file I/O errors, undermining its core purpose.

## MEDIUM Severity (12 items)

- [ ] **[1] ** — ``security/audit.go:109-113, 265-293``
  - Fix: (a) Add an `atomic.Int64` dropped-event counter and surface it in `GetStats()`. (b) Log `backgroundWriter` startup failures to stderr as a last-resort sink. (c) Collect exporter errors and expose via a callback or stats field. (small)

- [ ] **[2] ** — ``security/audit.go:99``
  - Fix: Use a monotonic counter: `atomic.AddUint64(&l.seq, 1)` appended to the timestamp, or use `uuid` / `crypto/rand`. (small)

- [ ] **[3] ** — ``ralph/loop.go:20-453``
  - Fix: Extract into focused methods: `executeIteration()`, `callSampler()`, `executeToolCalls()`, `recordAndCheck()`. Each becomes independently testable. The main loop becomes a ~50-line orchestrator. (large)

- [ ] **[4] ** — ``registry/registry.go:349-355``
  - Fix: Set `err = nil` after assigning the error result, or keep err for logging but document that panic recovery is the one exception. (small)

- [ ] **[5] ** — ``CLAUDE.md` (Package Map table, Dependency Layers section)`
  - Fix: Move both to a separate "Planned Packages" section, or remove from Package Map and Layers until implemented. (small)

- [ ] **[6] ** — ``transport/*.go`, `ROADMAP.md` Phase 30 summary`
  - Fix: Either add tests to bring transport to 90%+ (it's a thin adapter layer, should be straightforward), or update ROADMAP.md to note the exception. Transport was added in Phase 33 (after Phase 30), so the claim was true when written but is now misleading. (medium)

- [ ] **[7] ** — ``security/audit.go:258-263, 265-278``
  - Fix: Add a `sync.Once` for Close, and use a `sync.WaitGroup` or a done channel to wait for `backgroundWriter` to exit before closing exporters. (small)

- [ ] **[8] ** — ``rdcycle/filestore.go:68-89` (inferred from agent report)`
  - Fix: Collect errors and either return them as a second return value or log at warning level. (small)

- [ ] **[9] ** — ``session/session.go:177-194``
  - Fix: Accept an optional `context.Context` in `MemStoreOpts` and select on `ctx.Done()` alongside the done channel. Or document that `Close()` is mandatory and consider a finalizer warning. (small)

- [ ] **[10] ** — `Various — `gateway/upstream.go`, `handoff/handoff.go`, `auth/discovery.go``
  - Fix: Adopt a convention: all errors crossing package boundaries get `fmt.Errorf("pkg.func: %w", err)` wrapping. A lint rule (`wrapcheck` or custom) can enforce this. (medium)

- [ ] **[11] ** — ``registry/integrity.go:33-36``
  - Fix: Return an error from `Fingerprint()`, or fall back to `fmt.Sprintf("%v", td.Tool.InputSchema)` to ensure schema differences are always captured. (small)

- [ ] **[12] ** — ``ROADMAP.md``
  - Fix: Update counts or change to "all packages at time of writing" with a date qualifier. (small)

## Recommended Next Actions

- [ ] 1. **Fix audit logger silent failures** (`security/audit.go`) — add dropped-event counter, startup error logging, Close() synchronization. High impact on security observability, ~30 min effort.
- [ ] 2. **Update CLAUDE.md package accuracy** — remove `feedback`/`a2a` from implemented tables, fix package counts. Prevents agent confusion, ~15 min effort.
- [ ] 3. **Bring `transport` to 90% coverage** — thin adapter layer, mostly needs HTTP/WebSocket handler tests. Restores the "all packages 90%+" invariant, ~2 hours effort.
