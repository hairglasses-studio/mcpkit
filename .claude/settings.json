{
  "permissions": {
    "allow": [
      "Bash(go *)",
      "Bash(git *)",
      "Bash(make *)",
      "Read(*)",
      "Glob(*)",
      "Grep(*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(echo \"$TOOL_INPUT\" | grep -o '\"file_path\":\"[^\"]*\"' | head -1 | cut -d'\"' -f4); if [ -n \"$file_path\" ] && echo \"$file_path\" | grep -q '\\.go$'; then gofmt -w \"$file_path\" 2>/dev/null; fi"
          }
        ]
      }
    ]
  }
}
