#!/usr/bin/env bash
set -euo pipefail

if ! command -v pre-commit >/dev/null 2>&1; then
	echo "pre-commit is required for this hook. Install it with: uv tool install pre-commit" >&2
	exit 1
fi

exec pre-commit run --hook-stage pre-commit
