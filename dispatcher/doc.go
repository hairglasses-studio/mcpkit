// Package dispatcher provides a priority worker pool with concurrency groups
// for controlled parallel tool execution. It supports named concurrency groups
// with configurable limits, a FIFO/priority job queue, and middleware
// integration via the registry middleware signature.
package dispatcher
